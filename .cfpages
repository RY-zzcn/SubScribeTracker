# Cloudflare Pages 构建配置

# 构建设置
build:
  command: "npm run build:frontend"
  publish: "frontend/dist"
  environment:
    NODE_VERSION: "18"
    NPM_VERSION: "8"

# 环境变量（可选）
env:
  production:
    VITE_API_URL: "/api"